<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="br.com.fiap.mycontactlist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".form.ContactActivity" />
        <activity android:name=".form.NewContactActivity" />
        <activity android:name=".form.UpdateContactActivity" />
        <activity android:name=".list.ContactListActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".signup.SignupActivity" />
        <activity
                android:name=".splash.SplashActivity"
                android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>